[package]
name = "dnfdesk-gtk"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { package = "gtk4", version = "0.9" }
adw = { package = "libadwaita", version = "0.7" }
glib = "0.20"
dnfdesk-core = { path = "../dnfdesk-core" }
